export interface IElement {
    atomicNumber: number;
    symbol: string;
    name: string;
    atomicMass: any;
    electronegativity: Number,
    atomicRadius: Number,
    standardState: string,
    meltingPoint: Number,
    boilingPoint: Number,
    density: Number,
    groupBlock: string,
    yearDiscovered: Number,
    cpkHexColor: string
    period: number;
    group: number;
}
